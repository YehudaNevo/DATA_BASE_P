SELECT DISTINCT KS_NAME FROM  
KOSHER_SUPERVISOR NATURAL JOIN KOSHER_CERTIFICATE NATURAL JOIN KOSHER_RESTAURANT
WHERE KS_ID NOT IN (
SELECT DISTINCT KS_ID FROM
KOSHER_SUPERVISOR NATURAL JOIN KOSHER_CERTIFICATE NATURAL JOIN KOSHER_PRODACT)
